<h1>index invite_users</h1>

<%= form_for :users, url: invite_users_path do |f| %>
  <%= f.label :email %>
  <%= f.text_area :email %>

  <%= f.label :rank %>
  <%= f.text_area :rank %>

  <%= f.label :board_control %>
  <%= f.text_area :board_control %>

  <%= f.submit %>
<% end %>
<%= link_to :Back, post_index_path %>
